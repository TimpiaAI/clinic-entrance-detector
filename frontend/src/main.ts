import type { DashboardSnapshot } from './types.ts';
import './style.css';

// Placeholder: modules will be wired in Task 2
console.log('Clinic Entrance Detector frontend loaded');

// Ensure types are referenced (will be used by ws.ts in Task 2)
const _typeCheck: DashboardSnapshot | null = null;
void _typeCheck;
